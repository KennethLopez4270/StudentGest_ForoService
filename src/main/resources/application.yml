server:
  port: 8088

spring:
  application:
    name: foro-service
  datasource:
    url: jdbc:postgresql://localhost:5450/studentdb
    username: postgres
    password: ABCabc123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

# Configuración para comunicarse con student-service
student-service:
  base-url: http://localhost:8081/api/students